import React from 'react';
import ButtonSubscrib from '../../ButtonSubscrib/ButtonSubscrib';

const CanalSub = ({id}) => {
    return (
        <ButtonSubscrib
                id={id}
        />
    );
}

export default CanalSub;
